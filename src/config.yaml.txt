# src/config.yaml
data:
  csv_path: "../data/data.csv"
  image_base_path: "BMI/Data/Images"  # update if necessary

training:
  batch_size: 16
  epochs: 250
  optimizer_choices: ["Adam", "SGD"]
  learning_rates: [1e-5, 2e-5, 3e-5]   # simplified list for illustration
  early_stop_patience: 20
  weight_decay: 1e-5

model:
  load_pretrained: true
  freeze_base: false

logging:
  log_interval: 32
